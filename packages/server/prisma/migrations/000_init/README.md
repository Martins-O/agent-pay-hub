# Migration 000_init

Initial schema covering agents, invoices, payments, ledger events, webhook registrations, delivery attempts, and audit logs. The migration introduces enum types for invoice/payment status and webhook event types, aligning with the documented data model.
